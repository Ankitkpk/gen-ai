import React, { useState } from 'react'

const GeneratePayslipe = () => {
    const [isOpen , setIsOpen]=useState(false);
    const [loading ,setLoading]=useState(true);
  return (
    <div>GeneratePayslipe</div>
  )
}

export default GeneratePayslipe